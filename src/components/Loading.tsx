// Styles
import * as S from "@/styles/LoadingStyle";

const Loading = () => (
  <S.LoadingContainer>
    <S.LoadingSpinner>
      <S.PlanetIcon />
    </S.LoadingSpinner>
  </S.LoadingContainer>
);

export default Loading;
